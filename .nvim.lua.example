-- Example project-specific configuration file
-- Copy this file to .nvim.lua in your project root and customize as needed
-- WARNING: This file will be executed with full access to your system. Only use in trusted projects.

return {
	-- Override test adapters for specific languages
	test_overrides = {
		-- Example: Use playwright instead of vitest for TypeScript tests
		-- typescript = {
		-- 	adapter = "neotest-playwright",
		-- 	config = {
		-- 		options = {
		-- 			persist_project_selection = true,
		-- 			enable_dynamic_test_discovery = true,
		-- 			preset = "headed", -- or "debug" for debugging
		-- 		},
		-- 	},
		-- },

		-- Example: Custom pytest configuration
		-- python = {
		-- 	adapter = "neotest-python",
		-- 	config = {
		-- 		dap = { justMyCode = false },
		-- 		args = { "--log-level", "DEBUG" },
		-- 	},
		-- },
	},

	-- Override formatters for specific languages (future enhancement)
	-- formatter_overrides = {
	-- 	typescript = {
	-- 		{ "prettier", filetypes = { "typescript", "typescriptreact" } },
	-- 	},
	-- },

	-- Project-specific settings
	-- project_name = "My Project",
	-- root_markers = { ".git", "package.json" },
}
